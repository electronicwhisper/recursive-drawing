module.exports = {
  edgeSize: 0.7
  expansionLimit: 300
  minSize: 0.0000001 # this should get set to be 1/maxDimension where maxDimension is the largest dimension of the workspace canvas
  maxSize: 8
}